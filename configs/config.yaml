dataset: isic

segmentation:
  regions: 50

graph:
  k: 6

training:
  batch_size: 32
  epochs: 50
  lr: 0.001
  weight_decay: 0.0001

model:
  hidden_channels: 512
  heads: 2
  dropout: 0.3

logging:
  project: R-GNN
  use_wandb: true